apiVersion: v1
kind: Service
metadata:
  namespace: todos-api-production
  name: nginx
  labels:
    name: nginx
    project: todosapi
    environment: production
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: todosapi
    tier: nginx
  type: LoadBalancer
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  namespace: todos-api-production
  name: nginx
  labels:
    name: nginx
    project: todosapi
    environment: production
spec:
  template:
    metadata:
      labels:
        app: todosapi
        tier: nginx
    spec:
      containers:
        - image: asia.gcr.io/dprails5ondocker/nginx:1.0.0
          name: nginx
          lifecycle:
            preStop:
              exec:
                command: ["/usr/sbin/nginx","-s","quit"]